let installPrompt;
window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();installPrompt=e;});
if('serviceWorker' in navigator&&window.isSecureContext)navigator.serviceWorker.register('/sw.js').catch(()=>{});
document.addEventListener('click',async e=>{if(!e.target.closest('[data-install]'))return;if(installPrompt){await installPrompt.prompt();await installPrompt.userChoice;installPrompt=null;return;}const dialog=document.querySelector('#modal');dialog.classList.remove('studio-dialog');dialog.innerHTML=`<div class="dialog-head"><div><p class="eyebrow">MOOI, EVERY DAY</p><h2>A place on your home screen.</h2></div><button class="btn quiet" data-action="close" aria-label="Close">×</button></div><div class="dialog-body"><p><strong>iPhone:</strong> open MOOI in Safari, tap Share, then Add to Home Screen. Choose Open as Web App if shown.</p><p><strong>Android:</strong> open MOOI in Chrome, tap the menu, then Install app or Add to Home screen.</p><p><strong>Computer:</strong> open MOOI in Chrome or Edge and use the browser’s install option.</p><p class="notice">For your phone, use the hosted HTTPS MOOI link. A localhost link only works on the computer running the app. Installation does not make AI or account changes work offline.</p></div>`;if(!dialog.open)dialog.showModal();});
